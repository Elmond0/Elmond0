# Sostituisce languages.yml: quel workflow si era piantato su startup_failure
# ripetuti, con un file che actionlint validava come corretto e identico a uno
# che prima girava. Ricrearlo sotto un nome nuovo e' il modo per sbloccarlo.
name: Generate Charts

on:
  schedule:
    - cron: "30 7 * * *"
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/charts.yml"
      - ".github/scripts/langchart.py"
      - ".github/scripts/orbit.py"

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      # continue-on-error tiene i due grafici indipendenti: se uno fallisce
      # l'altro viene comunque generato e committato.
      - name: Build language donut chart
        continue-on-error: true
        env:
          GH_USER: ${{ github.repository_owner }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OUT: profile/languages.svg
        run: python3 .github/scripts/langchart.py

      - name: Build language orbit chart
        continue-on-error: true
        env:
          GH_USER: ${{ github.repository_owner }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ORBIT_OUT: profile/languages-orbit-v4.svg
        run: python3 .github/scripts/orbit.py

      - name: Commit charts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profile/languages.svg profile/languages-orbit-v4.svg
          git commit -m "chore: update language charts" || exit 0
          git pull --rebase --autostash
          git push
